name: territory-data-probe

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/territory-data-probe.yml"
      - "scripts/territory-data/**"
      - "src/data/sources/**"

permissions:
  contents: read

concurrency:
  group: territory-data-probe-main
  cancel-in-progress: true

jobs:
  census-complexo:
    runs-on: ubuntu-24.04
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Node 24 LTS
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Probe official census source
        env:
          TERRITORY_DATA_OUTPUT: territory-data-output
        run: node scripts/territory-data/probe-census-complexo.mjs

      - name: Upload census probe artifact
        uses: actions/upload-artifact@v4
        with:
          name: territory-census-complexo-probe
          path: territory-data-output
          if-no-files-found: error
          retention-days: 90

  public-places-metadata:
    runs-on: ubuntu-24.04
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Node 24 LTS
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Probe education and health source metadata
        env:
          TERRITORY_DATA_OUTPUT: territory-public-places-metadata
        run: node scripts/territory-data/probe-public-places-sources.mjs

      - name: Upload source metadata artifact
        uses: actions/upload-artifact@v4
        with:
          name: territory-public-places-source-metadata
          path: territory-public-places-metadata
          if-no-files-found: error
          retention-days: 90

  education-complexo:
    runs-on: ubuntu-24.04
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Node 24 LTS
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Probe official education places
        env:
          TERRITORY_DATA_OUTPUT: territory-education-output
        run: node scripts/territory-data/probe-education-complexo.mjs

      - name: Upload education probe artifact
        uses: actions/upload-artifact@v4
        with:
          name: territory-education-complexo-probe
          path: territory-education-output
          if-no-files-found: error
          retention-days: 90
