import { AccountSurfaceLoading } from '@/shared/layout/account-surface-loading';

export default function FavoritesLoading() {
  return <AccountSurfaceLoading active="favorites" label="Carregando Favoritos" variant="list" />;
}
